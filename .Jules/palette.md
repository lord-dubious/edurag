# Palette's Journal

## 2025-02-23 - Inconsistent component usage in Admin pages
**Learning:** Admin pages (specifically login) use raw HTML elements instead of design system components, leading to accessibility gaps (missing labels, focus states).
**Action:** Check other admin pages for similar patterns and replace with `components/ui` elements.
